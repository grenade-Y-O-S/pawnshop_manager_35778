<body class='interest_main'>
  <div class='title'>検索結果</div>
  <% if @pawns == nil %>
    <div class="nil_message">
      該当するデータはありませんでした。検索をやり直してください。
    </div>
  <% else %>
    <%= render partial: "shared/search-result-interest", collection: @pawns, as: "pawn" %>
  <% end %>

  <%= render partial: "shared/back-button" , locals: { back_link: interests_path } %>
</body>
